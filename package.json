{
  "name": "influencerai-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "prebuild": "pnpm install --frozen-lockfile",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "e2e:server": "tsx tests/e2e/mcv-server/server.ts",
    "e2e:test": "vitest run tests/e2e/specs --config tests/e2e/vitest.config.ts"
  },
  "devDependencies": {
    "express": "^4.21.2",
    "prettier": "^3.4.2",
    "puppeteer": "^23.11.1",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wait-on": "^8.0.1"
  },
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^3.3.2"
  }
}