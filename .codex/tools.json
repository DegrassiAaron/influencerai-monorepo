{
  "commands": {
    "install": "pnpm i",
    "build": "pnpm turbo run build",
    "up": "docker compose -f infra/docker/docker-compose.yml up -d --build",
    "down": "docker compose -f infra/docker/docker-compose.yml down",
    "logs_inference": "[Inference] docker compose -f infra/docker/docker-compose.yml logs -f SERVICE"
  },
  "constraints": [
    "Usare solo pnpm/turborepo/docker compose salvo richiesta esplicita.",
    "Etichettare contenuti non verificabili come [Inference]/[Unverified]."
  ]
}